#include<iostream>
using namespace std;
string replaceSpace(string &str){
    for(int i = 0;i<s.length();i++){
        string temp = "@40";
        if(str[i]==' '){
            s[i]==temp;
        }
    }
    
}

int main(){
    string s;
    cin.getline(s,100);
    replaceSpace(s);
    cout<<s<<endl;
}